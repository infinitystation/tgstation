{{#each data.alarms:class}}
  <ui-display title='{{class}} Тревоги'>
    <ul>
      {{#each .}}
        <li>{{.}}</li>
      {{else}}
        <li>Системы в норме</li>
      {{/each}}
    </ul>
  </ui-display>
{{/each}}
